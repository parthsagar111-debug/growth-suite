streamlit>=1.36
plotly>=5.22
requests>=2.32
supabase>=2.5
python-dotenv>=1.0
pandas>=2.0
# Pinned exactly — 0.11.0 breaks with `Client.__init__() got an unexpected
# keyword argument 'proxies'` once a newer httpx drops that argument.
groq==1.5.0
